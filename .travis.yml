language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules

script:
  - npm run build
  - cp CNAME dist/ && bash copy_links.sh public dist
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  keep_history: true
  on:
    branch: master